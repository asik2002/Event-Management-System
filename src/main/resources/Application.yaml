spring:
  datasource:
    url: ${db_url}
    username: ${db_user}
    password: ${db_pass}
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    show-sql: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
server:
  port: 8080

